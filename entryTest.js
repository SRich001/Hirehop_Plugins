$(document).ready(function()
	// Adds a Depot Dropdown to the page
	{
		//Get all Depots from depots array and put them in the dep object
		//Watch for the depot box to change and update the users depot then reload the page
		
		// Fill it with depots
	
        // Insert the select in place of the depot labe
        $("<span>",{ html:" Hello"}).insertAfter($("#search_by_number_content"));
        
	});

